import styled from 'styled-components';

export const TimeDisplayBox = styled.div`
  font-size: 3.5rem;
  font-weight: bold;
  background: #2c3e50;
  color: #ecf0f1;
  padding: 1rem 2rem;
  border-radius: 15px;
  margin: 1.5rem 0;
  font-family: 'Courier New', monospace;
  letter-spacing: 5px;
  box-shadow: inset 0 0 10px rgba(0, 0, 0, 0.5);
`;